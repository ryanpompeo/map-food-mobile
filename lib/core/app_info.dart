/// Versão exibida no rodapé da tela de Perfil — mantida manualmente em
/// sincronia com o campo `version:` do pubspec.yaml. Não usamos
/// package_info_plus pra evitar puxar uma dependência nova só pra um rótulo.
const String kAppVersion = '1.0.0 (1)';
