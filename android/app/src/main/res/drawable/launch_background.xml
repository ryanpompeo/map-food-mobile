<?xml version="1.0" encoding="utf-8"?>
<!-- Splash nativa (API < 21). Com minSdk 24 este arquivo nunca chega a ser
     usado — existe só porque é o default que o resolvedor de recursos procura
     quando não há qualificador. A variante viva é
     drawable-v21/launch_background.xml; em Android 12+, values-v31/styles.xml. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher" />
    </item>
</layer-list>
