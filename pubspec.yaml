name: map_food
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.9
  # google_fonts removido: nenhuma referência a `GoogleFonts` em lib/. A
  # tipografia do app é a família Inter declarada em `fonts:` no fim deste
  # arquivo e consumida por `core/ui/theme/app_typography.dart`.
  cpf_cnpj_validator: ^2.0.0
  mask_text_input_formatter: ^2.9.0
  dio: ^5.8.0+1
  shared_preferences: ^2.5.3
  flutter_markdown: 0.7.7+1
  image_picker: ^1.1.2
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  flutter_map: ^7.0.2
  flutter_map_cancellable_tile_provider: ^3.0.2
  latlong2: ^0.9.1
  phosphoricons_flutter: ^1.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter



  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# O bloco `flutter_icons:` que existia aqui foi removido: era a configuração do
# `flutter_dynamic_launcher_icon` e apontava para `assets/images/isologo.png`,
# enquanto o `flutter_launcher_icons:` no fim do arquivo aponta para
# `assets/icon/app_icon.png`. Com os dois geradores instalados, o ícone final
# dependia de qual comando tinha rodado por último. Ficou só o segundo.

# Força path_provider_foundation para a última versão sem objective_c.
# objective_c foi introduzido em 2.5.0 e causa falha de compilação
# quando o Flutter SDK está instalado em caminho com espaços (Windows).
#
# ATENÇÃO: este override congela um plugin **Darwin** por causa de um problema
# de ambiente **Windows**. Num build iOS de CI ele silenciosamente entrega uma
# versão defasada. Condição de saída: remover assim que o Flutter SDK desta
# máquina deixar de morar num caminho com espaços — e então testar
# `flutter build ios` antes de fechar.
dependency_overrides:
  path_provider_foundation: 2.4.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/category/
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter_18pt-Regular.ttf
        - asset: assets/fonts/Inter_18pt-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter_18pt-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter_18pt-Bold.ttf
          weight: 700
  # A família acima é a única fonte do app (ver core/ui/theme/app_typography.dart).
  # O comentário anterior aqui dizia que o app usava Poppins via google_fonts —
  # isso deixou de valer na v2 do design system, quando a Inter passou a ser
  # empacotada em assets/ (ver o cabeçalho de core/ui/theme/app_theme.dart).

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/app_icon.png"
  min_sdk_android: 21 


  adaptive_icon_background: "#FFFFFF" # Cor hexadecimal do fundo (branco)
  adaptive_icon_foreground: "assets/icon/app_icon.png"
